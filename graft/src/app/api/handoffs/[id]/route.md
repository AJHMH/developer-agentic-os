# src/app/api/handoffs/[id]/route.ts

- GET · function · L8-L18 — async function GET(request: Request, { params }: { params: Promise<{ id: string }> })
- PATCH · function · L20-L40 — async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> })
